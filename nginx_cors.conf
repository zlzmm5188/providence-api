# Nginx CORS配置示例
# 将此配置添加到您的Nginx server块中

# 处理OPTIONS预检请求
location /api {
    # CORS Headers
    add_header 'Access-Control-Allow-Origin' '$http_origin' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS, PATCH' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Token, X-Requested-With, Accept, Origin' always;
    add_header 'Access-Control-Allow-Credentials' 'true' always;
    add_header 'Access-Control-Max-Age' '1800' always;

    # 处理OPTIONS预检请求
    if ($request_method = 'OPTIONS') {
        add_header 'Access-Control-Allow-Origin' '$http_origin' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS, PATCH' always;
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Token, X-Requested-With, Accept, Origin' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        add_header 'Access-Control-Max-Age' '1800' always;
        add_header 'Content-Length' '0' always;
        add_header 'Content-Type' 'text/plain' always;
        return 204;
    }

    # 代理到PHP-FPM
    try_files $uri $uri/ /index.php?$query_string;
    fastcgi_pass unix:/var/run/php/php-fpm.sock;
    fastcgi_index index.php;
    fastcgi_param SCRIPT_FILENAME $document_root/index.php;
    include fastcgi_params;
}

# 或者更简单的全局CORS配置（允许所有来源）
# location /api {
#     if ($http_origin ~* "^https?://(localhost|127\.0\.0\.1|.*\.4kp3l0iq\.top)(:[0-9]+)?$") {
#         add_header 'Access-Control-Allow-Origin' '$http_origin' always;
#         add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS, PATCH' always;
#         add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Token, X-Requested-With, Accept, Origin' always;
#         add_header 'Access-Control-Allow-Credentials' 'true' always;
#         add_header 'Access-Control-Max-Age' '1800' always;
#     }
#
#     if ($request_method = 'OPTIONS') {
#         return 204;
#     }
#
#     try_files $uri $uri/ /index.php?$query_string;
#     fastcgi_pass unix:/var/run/php/php-fpm.sock;
#     fastcgi_index index.php;
#     fastcgi_param SCRIPT_FILENAME $document_root/index.php;
#     include fastcgi_params;
# }
